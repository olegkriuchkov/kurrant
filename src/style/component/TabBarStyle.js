import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  tabBar: {
    flex: 1,
    borderColor: 'black',
    height: 100,
    width: '100%',
    backgroundColor: 'black',
  },
});
